# Copy to secrets.toml for local runs (never commit secrets.toml).
# On Streamlit Cloud: App settings → Secrets → paste the same keys.

OPENAI_API_KEY = "sk-your-openai-key"
APP_PASSWORD = "your-class-password"
